name: CI

on:
  pull_request:
    # S'exécute sur toutes les pull requests
  push:
    branches-ignore:
      - main
      - master
    # S'exécute sur toutes les branches sauf main et master

jobs:
  quality-check:
    name: Vérification du code
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout du code
        uses: actions/checkout@v4

      - name: 📦 Configuration de Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: 📥 Installation des dépendances
        run: npm ci

      - name: 🔍 Vérification du linting (ESLint)
        run: npm run lint

      - name: 🔷 Vérification des types (TypeScript)
        run: npm run type-check

      - name: 🏗️ Vérification du build
        run: npm run build
        env:
          # Variables d'environnement pour le build (ajustez selon vos besoins)
          NEXT_PUBLIC_SITE_URL: "http://localhost:3000"


